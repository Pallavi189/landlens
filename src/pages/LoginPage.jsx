import PlaceholderPage from "./PlaceholderPage";

function LoginPage() {
  return (
    <PlaceholderPage
      eyebrow="Login"
      title="Authentication is planned for a later milestone."
      body="For now, the admin page is a preview without full authentication. Supabase auth can be added when roles and access rules are finalized."
    />
  );
}

export default LoginPage;
